#include "src/features/map.h"
#include "src/renderer.h"

int main() {
    /*
    int sideSize = 8; // Taille de la carte
    int rewindLimit = 5;
    Map gameMap(sideSize, rewindLimit);

    // Ajouter des insectes pour tester
    Ant Ant(true,ant);
    Bee Bee(true,bee);

    gameMap.putInsect(&Ant, vec2i(3, 4));
    gameMap.putInsect(&Bee, vec2i(2, 2));

    // Afficher la carte
    Renderer renderer(gameMap);
    renderer.displayMap();*/

    return 0;
}
